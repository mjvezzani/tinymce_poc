<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br/>
  <%= f.label :body %>
  <%= f.text_area :body, :class => "tinymce", :rows => 20, :cols =>20 %>
  <br/>
  <%= f.submit %>
<% end %>

<%= tinymce %>
